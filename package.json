{
  "name": "darfest2026",
  "version": "1.0.0",
  "description": "DarFest 2026 Result Platform with Telegram Integration",
  "main": "server.js",
  "bin": {
    "npmstart": "./npmstart"
  },
  "scripts": {
    "start": "node server.js",
    "npmstart": "node server.js",
    "build": "mkdir -p node_modules/.bin && cp npmstart node_modules/.bin/npmstart && chmod +x node_modules/.bin/npmstart || true",
    "postinstall": "mkdir -p node_modules/.bin && cp npmstart node_modules/.bin/npmstart && chmod +x node_modules/.bin/npmstart || true"
  },
  "keywords": ["artfest", "darfest", "telegram-bot"],
  "author": "Jabir Vettan",
  "license": "ISC"
}
